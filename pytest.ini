[pytest]
markers =
    live_llm: makes real LLM/API calls; needs OPENAI_API_KEY. Deselected by default.
    live_server: needs a running server at localhost:8000. Deselected by default.
addopts = -q -m "not live_llm and not live_server"
